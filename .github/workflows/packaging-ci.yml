name: Packaging CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

concurrency:
  group: packaging-ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  deb-aur-ubuntu:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.11"

      - name: Preflight metadata and script checks
        run: |
          set -euo pipefail
          ! grep -R -E 'example\.invalid|noreply@example\.invalid' packaging automation || {
            echo "Placeholder metadata found in packaging or automation helpers."
            exit 1
          }
          bash -n automation/*.sh

      - name: Build DEB and AUR artifacts
        run: |
          set -euo pipefail
          make package-deb
          make package-aur

      - name: Smoke test artifacts
        run: |
          set -euo pipefail
          bash automation/test_packages.sh

      - name: Upload artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: deb-aur-artifacts
          path: dist/
          if-no-files-found: error
          retention-days: 14

  rpm-fedora:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    container:
      image: fedora:41
    steps:
      - name: Install dependencies
        run: |
          set -euo pipefail
          dnf -y install git make python3 python3-pip rpm-build findutils tar gzip coreutils

      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Preflight metadata and script checks
        run: |
          set -euo pipefail
          ! grep -R -E 'example\.invalid|noreply@example\.invalid' packaging automation || {
            echo "Placeholder metadata found in packaging or automation helpers."
            exit 1
          }
          bash -n automation/*.sh

      - name: Build RPM artifact
        run: |
          set -euo pipefail
          make package-rpm

      - name: Smoke test RPM artifact
        run: |
          set -euo pipefail
          bash automation/test_packages.sh

      - name: Upload artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: rpm-artifacts
          path: dist/
          if-no-files-found: error
          retention-days: 14

  aur-arch-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    container:
      image: archlinux:base-devel
    steps:
      - name: Install dependencies
        run: |
          set -euo pipefail
          pacman -Syu --noconfirm
          pacman -S --noconfirm git make python

      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Preflight metadata and script checks
        run: |
          set -euo pipefail
          ! grep -R -E 'example\.invalid|noreply@example\.invalid' packaging automation || {
            echo "Placeholder metadata found in packaging or automation helpers."
            exit 1
          }
          bash -n automation/*.sh

      - name: Build AUR bundle
        run: |
          set -euo pipefail
          make package-aur

      - name: Smoke test AUR bundle
        run: |
          set -euo pipefail
          bash automation/test_packages.sh

      - name: Validate PKGBUILD syntax
        run: |
          set -euo pipefail
          bash -n dist/aur/PKGBUILD
          grep -q '^pkgname=firefox-installer' dist/aur/PKGBUILD

      - name: Upload artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: aur-artifacts
          path: dist/aur/
          if-no-files-found: error
          retention-days: 14
